# frozen_string_literal: true

source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

# gem "rails"
gem 'colorize'
gem 'pry'


# bundle init creates Gemfile
# how to do we add to project?
  # run "bundle"
# add to code? 
  # require 'colorize'